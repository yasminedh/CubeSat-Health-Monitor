import 'package:flutter/material.dart';

/// Shared design tokens — import this in every file instead of redefining _C.
class AppColors {
  static const bg           = Color(0xFF111217);
  static const panel        = Color(0xFF181B1F);
  static const border       = Color(0xFF2C2F35);
  static const header       = Color(0xFF1F2228);
  static const sidePanel    = Color(0xFF13161C);

  static const textPrimary  = Color(0xFFD8D9DA);
  static const textSecondary= Color(0xFF9FA3A8);
  static const textDim      = Color(0xFF5A5E66);

  static const accentBlue   = Color(0xFF5794F2);
  static const accentGreen  = Color(0xFF73BF69);
  static const accentOrange = Color(0xFFFF9830);
  static const accentRed    = Color(0xFFF2495C);
  static const accentTeal   = Color(0xFF4ECDC4);
  static const accentYellow = Color(0xFFEAB839);
  static const accentPurple = Color(0xFF9C6FE4);

  static const gridLine     = Color(0xFF22262D);
  static const separator    = Color(0xFF2C2F35);
}